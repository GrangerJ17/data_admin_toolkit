## All these files are practically unused at this stage

They will be used once html parsing is needed (e.g. Cobden & Hayson website has no API endpoint hydrating frontend (at least i cant find it) so html parse will be needed and a normalisation/cleaning step is needed)