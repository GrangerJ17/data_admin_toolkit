pipeline {
  agent {label "linux"}
  stages {
    stage ('Initial'){
      steps {
        echo "Testing extraction, scraping, transforming, embedding and embedding query logic with test :)"
      }
    }
  }
}
